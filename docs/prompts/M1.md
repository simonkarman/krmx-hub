# M1 — Identity, approval gate, roles, admin dashboard

Read docs/ARCHITECTURE.md §2, §5 (participant), §9.7/9.9 and CLAUDE.md.

Goal: sign in locally, land in pending, admin approves and grants roles/credits.

Deliver:
- Auth.js v5 with a DEV-ONLY credentials provider (NODE_ENV guard; real OAuth
  is M6). Session carries status + roles (module augmentation).
- pending → approved/blocked flow; ADMIN_EMAIL bootstrap grants admin role.
- /admin: approve/reject/revoke, grant/revoke host+admin roles, grant credits
  (writes `grant` ledger rows).
- Security tests: matrix rows A-01, A-02, A-05, A-08, A-09.

Done when: full manual flow works locally; `pnpm test:security` green.
Review focus: authorization re-checks inside every server action/route.
