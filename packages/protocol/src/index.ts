/**
 * @hub/protocol — the platform API surface (types only).
 *
 * M2 fills this in: ticket claims, postMessage message schemas (zod),
 * and service API shapes. See docs/ARCHITECTURE.md §4 and §7.
 */
export const PROTOCOL_VERSION = 1 as const;
