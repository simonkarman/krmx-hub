/**
 * @hub/game-server-sdk — placeholder.
 *
 * M2 fills this in: verifyTicket(jwt, expectedAud) via cached JWKS with an
 * explicit RS256 allowlist and a single-use jti seen-set, plus
 * HubServiceClient (service token passed per-call).
 */
export {};
