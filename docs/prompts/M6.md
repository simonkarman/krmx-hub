# M6 — Phase 1: real identity + cloud hub

Read docs/ARCHITECTURE.md §3, §12. This milestone swaps exactly one seam:
localhost identity/db/hosting -> Google OAuth + Neon + Netlify.

Deliver:
- Google provider + Resend magic links (dev credentials provider stays,
  NODE_ENV-guarded); Neon pooled DATABASE_URL; Netlify deploy config for
  apps/hub; env documentation (.env.example) for every secret.
- Signing key from env (document rotation via kid).
- No architecture changes. All existing tests stay green locally.

Review focus: env/secret wiring; nothing else should have changed.
