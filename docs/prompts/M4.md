# M4 — Credit ledger: holds, settlement, conservation

Read docs/ARCHITECTURE.md §5 (ledger + invariants), §6.2, §6.4 and
SECURITY-TEST-PLAN.md §2 L rows.

Goal: money is sound.

Deliver:
- entry_hold on create/join (balance check in the same transaction);
  join endpoint with capacity + idempotency.
- POST /api/service/instances/:id/results: validate ranking against
  instance_player, settle atomically (capture holds, write payouts), mark
  finished, revoke token. Idempotent under replay.
- Reaper completes: hold_release on cancel.
- Balances in UI (SUM over ledger).
- Security tests: ALL L rows including L-06 (concurrency race) and L-08
  (fast-check property test over random interleavings), S-06.

Done when: property tests pass repeatedly (run with several seeds).
Review focus: transaction boundaries; the conservation property test itself.
