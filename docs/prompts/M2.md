# M2 — Protocol package, ticket minting, JWKS

Read docs/ARCHITECTURE.md §4, §7 and SECURITY-TEST-PLAN.md §2 (T, H) first.

Goal: the hub is an identity provider for game servers.

Deliver:
- packages/protocol: ticket claims, postMessage message types (zod schemas),
  service API shapes. THIS IS THE PLATFORM API — keep it minimal and exact.
- RSA keypair handling (env for dev, test fixtures for tests);
  /.well-known/jwks.json with kid.
- GET /api/instances/:id/ticket: membership + approved check, 2-min RS256
  ticket with jti, rate limited (A-10).
- packages/game-server-sdk: verifyTicket(jwt, expectedAud) via cached JWKS,
  explicit RS256 allowlist, jti single-use seen-set.
- Security tests: ALL T rows (T-01..T-11), A-03, A-04, A-10, H-01, H-02.

Done when: tests green, including T-02 (algorithm confusion) and T-08 (replay).
Review focus: packages/protocol types; the verify function; test cases.
