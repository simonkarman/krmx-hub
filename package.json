{
  "name": "krmx-hub",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "db:up": "docker compose up -d",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:security": "pnpm --filter security test"
  }
}
