# Browser (Playwright) security tests

Empty until M5, which adds the two-origin harness (hub :3000, game frontend
:4000, evil origin :4666) and the F-row specs from
docs/SECURITY-TEST-PLAN.md §2. Spec files must carry their matrix IDs in test
titles (e.g. `F-01`) so the matrix-coverage meta-test can find them.
