# M7 — Phase 2: real Cloud Run provisioner

Read docs/ARCHITECTURE.md §6.1 and the Deferred list (do NOT build multiplexing).

Goal: examples/tictactoe/provisioner deploys real per-instance servers.

Deliver:
- Dockerfile for the tictactoe server; image pushed version-tagged to
  Artifact Registry (document commands).
- Provisioner (Cloud Run service or function) calling the Cloud Run Admin API:
  deploy pinned image, --min-instances 0 --max-instances 1 --timeout 3600
  --session-affinity; returns wss:// URL; teardown on settle/reap.
- Reconnect flow survives the 60-min WebSocket cap (fresh ticket + relink).
- Document the provisioner service account (least privilege around run.admin).

Review focus: service-account scope; image pinning (never :latest).
