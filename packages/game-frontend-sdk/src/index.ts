/**
 * @hub/game-frontend-sdk — placeholder.
 *
 * M5 fills this in: connectToHub() postMessage handshake with pinned origins,
 * hub:request-ticket refresh, zod-parsing of every inbound message.
 * See docs/ARCHITECTURE.md §7.
 */
export {};
