/**
 * tictactoe provisioner — placeholder.
 *
 * M3 fills this in: local HTTP endpoint that verifies the hub's HMAC-signed
 * provision call, spawns a server child process, and returns
 * ws://localhost:<port>. Cloud Run provisioning is M7.
 */
export {};
