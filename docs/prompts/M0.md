# M0 — Monorepo skeleton

Read docs/ARCHITECTURE.md (§8 packages, §12 tech pins) and CLAUDE.md first.

Goal: pnpm workspace boots end to end on localhost.

Deliver:
- Workspace packages scaffolded per CLAUDE.md layout (empty-but-typed:
  tsconfig, package.json, placeholder index per package; changesets config).
- apps/hub: Next.js ^15 App Router app boots with a placeholder page.
- db: schema from ARCHITECTURE.md §5 as SQL migration(s) + `pnpm db:migrate`
  script; applies cleanly against docker-compose Postgres.
- security/ package with vitest + playwright configured; one passing smoke
  test; the matrix-coverage meta-test stubbed (asserts test-title IDs against
  SECURITY-TEST-PLAN.md, allowed to pass with zero rows for now).

Done when: `pnpm db:up && pnpm db:migrate && pnpm build && pnpm test` green.
Review focus: repo layout, schema SQL.
