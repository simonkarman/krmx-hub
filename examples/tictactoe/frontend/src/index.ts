/**
 * tictactoe frontend — placeholder.
 *
 * M5 fills this in: static frontend served on localhost:4000, embedded by the
 * hub's <PlayFrame>, using @hub/game-frontend-sdk for the handshake.
 */
export {};
