# M5 — Play end to end: iframe shell, frontend SDK, tictactoe

Read docs/ARCHITECTURE.md §6.3, §7 and SECURITY-TEST-PLAN.md §2 F rows + §3.

Goal: full flow on localhost: create -> join -> play -> results -> balances.

Deliver:
- apps/hub <PlayFrame>: sandboxed iframe, src from REGISTERED frontend_url
  only, origin-pinned postMessage broker, CSP frame-src allowlist.
- packages/game-frontend-sdk: connectToHub() handshake incl. hub:request-ticket
  refresh; zod-parse every inbound message.
- packages/krmx-adapter: authenticate hook wiring (markAsync) using
  game-server-sdk verifyTicket.
- examples/tictactoe: static frontend (localhost:4000) + Krmx server; playable
  2-player game that posts results.
- Playwright harness per SECURITY-TEST-PLAN §3 incl. evil origin :4666.
- Security tests: ALL F rows, T-08 end-to-end, H-04.

Done when: two browser contexts play a full game; credits settle; F tests green.
Review focus: origin pinning both directions; the adapter (~15 lines).
